main: ${PACKAGE}.${NAME}
name: ${NAME}
version: "${VERSION}"
api-version: "1.18"
author: rndtrash
description: A simple MOTD plugin
commands:
  motd:
    description: Show the message of the day
    usage: "/motd and that's it."